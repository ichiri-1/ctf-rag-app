# claude code のローカルでの設定 settings.local.json.example -> settings.local.json へ変更する
{
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "env": {
        "CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR": "1"
    }
}
